const Router = require('koa-router');

const router = module.exports = new Router({ prefix: '/agent' });

router.get('/:agentId', ctx => {

});

router.post('/', ctx => {

});

router.get('/:agentId/program', ctx => {

});

router.post('/:agentId/program/:programHash/return', ctx => {

});